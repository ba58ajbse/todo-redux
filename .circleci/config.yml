version: 2.1

orbs:
  cypress: cypress-io/cypress@1

jobs:
  build:
    docker:
      - image: circleci/node:12.18.3
    steps:
      - checkout
      - cypress/run

workflows:
  version: 2
  setting_test:
    jobs:
      - build