version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:12.18.3
    steps:
      - checkout
      - run: echo "Hello, World!"

workflows:
  version: 2
  setting_test:
    jobs:
      - build:
          filters:
            branches:
              only: setting-circleci